# This is a basic workflow to help you get started with Actions

name: Hello World

# Controls when the workflow will run
on:
 workflow_dispatch:

jobs:
  Tarea1:
    runs-on: ubuntu-latest

    steps:
      - name: Di Hola
        run: echo "Francisco Rodriguez"
